import { MapMLGL } from "@maptiler/sdk";
import { ColorRamp } from "./colorramp";
import { TileLayer } from "./tilelayer";
/**
 * Type as returned by the `.pickAt()` method
 */
export type TemperaturePickAt = {
    /**
     * Temperature in degree Celcius
     */
    value: number;
    /**
     * Temperature in degree Farenheit
     */
    valueImperial: number;
} | null;
export type TemperatureLayerOptions = {
    /**
     * ID of the layer
     */
    id?: string;
    /**
     * Opacity of the layer in [0, 1]
     */
    opacity?: number;
    /**
     * Colormap to use
     */
    colorramp?: ColorRamp;
    /**
     * Whether or not the colorramp must be smooth
     */
    smooth?: boolean;
};
/**
 * The TemperatureLayer shows the atmospheric temperature in centigrade
 */
export declare class TemperatureLayer extends TileLayer {
    private constructorOptions;
    private isSourceReady;
    private colorRamp;
    constructor(options?: TemperatureLayerOptions);
    onAdd(map: MapMLGL, gl: WebGLRenderingContext | WebGL2RenderingContext): Promise<void>;
    /**
     * Check if the data source that fuels the layer has been fully fetched and processed
     * @returns
     */
    getIsSourceReady(): boolean;
    /**
     * Get the temperature in multiple measurement units at a given location
     * @param lng
     * @param lat
     * @returns
     */
    pickAt(lng: number, lat: number): TemperaturePickAt;
    /**
     * Get the color ramp in use by the layer
     * @returns
     */
    getColorRamp(): Readonly<ColorRamp>;
}
