import { MapMLGL } from "@maptiler/sdk";
import { ColorRamp } from "./colorramp";
import { TileLayer } from "./tilelayer";
/**
 * Type as returned by the `.pickAt()` method
 */
export type PrecipitationPickAt = {
    /**
     * Amount of precipitation in mm/h
     */
    value: number;
    /**
     * Amount of precipitation in in/h
     */
    valueImperial: number;
};
export type PrecipitationLayerOptions = {
    /**
     * ID of the layer
     */
    id?: string;
    /**
     * Opacity of the layer in [0, 1]
     */
    opacity?: number;
    /**
     * Colormap to use
     */
    colorramp?: ColorRamp;
    /**
     * Whether or not the colorramp must be smooth
     */
    smooth?: boolean;
};
/**
 * The PrecipitationLayer shows the atmospheric precipitation in millibar (or hectopascal)
 */
export declare class PrecipitationLayer extends TileLayer {
    private constructorOptions;
    private isSourceReady;
    private colorRamp;
    constructor(options?: PrecipitationLayerOptions);
    onAdd(map: MapMLGL, gl: WebGLRenderingContext | WebGL2RenderingContext): Promise<void>;
    /**
     * Check if the data source that fuels the layer has been fully fetched and processed
     * @returns
     */
    getIsSourceReady(): boolean;
    /**
     * Get the amount of precipitation in mm/h
     * @param lng
     * @param lat
     * @returns
     */
    pickAt(lng: number, lat: number): PrecipitationPickAt | null;
    /**
     * Get the color ramp in use by the layer
     * @returns
     */
    getColorRamp(): Readonly<ColorRamp>;
}
