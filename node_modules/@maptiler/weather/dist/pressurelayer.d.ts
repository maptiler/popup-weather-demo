import { MapMLGL } from "@maptiler/sdk";
import { ColorRamp } from "./colorramp";
import { TileLayer } from "./tilelayer";
/**
 * Type as returned by the `.pickAt()` method
 */
export type PressurePickAt = {
    /**
     * Air pressure in hPa (or mbar)
     */
    value: number;
};
export type PressureLayerOptions = {
    /**
     * ID of the layer
     */
    id?: string;
    /**
     * Opacity of the layer in [0, 1]
     */
    opacity?: number;
    /**
     * Colormap to use
     */
    colorramp?: ColorRamp;
    /**
     * Whether or not the colorramp must be smooth
     */
    smooth?: boolean;
};
/**
 * The PressureLayer shows the atmospheric pressure in millibar (or hectopascal)
 */
export declare class PressureLayer extends TileLayer {
    private constructorOptions;
    private isSourceReady;
    private colorRamp;
    constructor(options?: PressureLayerOptions);
    onAdd(map: MapMLGL, gl: WebGLRenderingContext | WebGL2RenderingContext): Promise<void>;
    /**
     * Check if the data source that fuels the layer has been fully fetched and processed
     * @returns
     */
    getIsSourceReady(): boolean;
    /**
     * Get the air pressure in hPa at a given location
     * @param lng
     * @param lat
     * @returns
     */
    pickAt(lng: number, lat: number): PressurePickAt | null;
    /**
     * Get the color ramp in use by the layer
     * @returns
     */
    getColorRamp(): Readonly<ColorRamp>;
}
